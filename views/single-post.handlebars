<article class="post my-3 mx-4">
    <div class="title-bar">
        <h4>{{post.title}}</h4>
        <h6 class="side-title">Posted by {{post.user.username}} on {{post.created_at}}</h6>
    </div>
    <div class="contents" >
        <p>{{post.contents}}</p>
    </div>
</article>